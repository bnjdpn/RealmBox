From: RealmBox
Subject: [PATCH] Prioritize player replies, invalidate stale ambient chat, and back off inference failures
Upstream-Commit: a9d14b0b8955be136e657ac168dd255f5281a535

diff --git a/src/mod-ollama-chat_api.cpp b/src/mod-ollama-chat_api.cpp
index 0f8840e..93d6e91 100644
--- a/src/mod-ollama-chat_api.cpp
+++ b/src/mod-ollama-chat_api.cpp
@@ -8,12 +8,124 @@

 #include <algorithm>
 #include <chrono>
+#include <limits>
 #include <nlohmann/json.hpp>
 #include <mutex>
 #include <sstream>

 namespace
 {
+    // REALMBOX_BACKOFF_POLICY_BEGIN
+    // Pure policy: monotonic milliseconds are injected, and callers serialize
+    // access. No sleeps, retries, threads, I/O, or game objects belong here.
+    class RealmBoxOllamaBackoff
+    {
+    public:
+        struct Permit
+        {
+            bool allowed = false;
+            uint64_t generation = 0;
+            bool probe = false;
+        };
+
+        Permit Acquire(uint64_t nowMs)
+        {
+            if (!open_)
+                return { true, generation_, false };
+            if (probeInFlight_ || deadlineSaturated_ || nowMs < retryAtMs_)
+                return {};
+            probeInFlight_ = true;
+            return { true, generation_, true };
+        }
+
+        void Complete(Permit permit, bool success, uint64_t nowMs)
+        {
+            // Completions admitted before an opening/reset cannot close or
+            // extend the new circuit state, even with several worker threads.
+            if (!permit.allowed || permit.generation != generation_)
+                return;
+            if (permit.probe)
+            {
+                if (!open_ || !probeInFlight_)
+                    return;
+                if (success)
+                    Reset();
+                else
+                    Open(nowMs, true);
+                return;
+            }
+            if (open_)
+                return;
+            if (success)
+                failures_ = 0;
+            else if (++failures_ >= FailureThreshold)
+                Open(nowMs, false);
+        }
+
+        void Reset()
+        {
+            ++generation_;
+            failures_ = 0;
+            cooldownMs_ = InitialCooldownMs;
+            retryAtMs_ = 0;
+            open_ = false;
+            probeInFlight_ = false;
+            deadlineSaturated_ = false;
+        }
+
+    private:
+        static constexpr uint32_t FailureThreshold = 3;
+        static constexpr uint64_t InitialCooldownMs = 5000;
+        static constexpr uint64_t MaximumCooldownMs = 60000;
+
+        void Open(uint64_t nowMs, bool increase)
+        {
+            if (increase)
+                cooldownMs_ = cooldownMs_ > MaximumCooldownMs / 2
+                    ? MaximumCooldownMs : cooldownMs_ * 2;
+            ++generation_;
+            failures_ = FailureThreshold;
+            open_ = true;
+            probeInFlight_ = false;
+            const uint64_t maximum = std::numeric_limits<uint64_t>::max();
+            deadlineSaturated_ = nowMs > maximum - cooldownMs_;
+            retryAtMs_ = deadlineSaturated_ ? maximum : nowMs + cooldownMs_;
+            // A clock overflow fails closed until an explicit config reset;
+            // wrapping the deadline would otherwise create an unbounded loop.
+        }
+
+        uint64_t generation_ = 1;
+        uint32_t failures_ = 0;
+        uint64_t cooldownMs_ = InitialCooldownMs;
+        uint64_t retryAtMs_ = 0;
+        bool open_ = false;
+        bool probeInFlight_ = false;
+        bool deadlineSaturated_ = false;
+    };
+    // REALMBOX_BACKOFF_POLICY_END
+
+    std::mutex g_backoffMutex;
+    RealmBoxOllamaBackoff g_backoff;
+
+    uint64_t BackoffNowMs()
+    {
+        const auto milliseconds = std::chrono::duration_cast<std::chrono::milliseconds>(
+            std::chrono::steady_clock::now().time_since_epoch()).count();
+        return milliseconds < 0 ? 0 : static_cast<uint64_t>(milliseconds);
+    }
+
+    struct BackoffCompletion
+    {
+        RealmBoxOllamaBackoff::Permit permit;
+        bool successful = false;
+
+        ~BackoffCompletion()
+        {
+            std::lock_guard<std::mutex> lock(g_backoffMutex);
+            g_backoff.Complete(permit, successful, BackoffNowMs());
+        }
+    };
+
     // One client per worker thread; the client itself pools keep-alive sockets.
     thread_local OllamaHttpClient t_httpClient;

@@ -230,8 +342,14 @@ void OllamaConfig_Publish()
     next.presencePenalty  = g_OllamaPresencePenalty;
     next.frequencyPenalty = g_OllamaFrequencyPenalty;

-    std::lock_guard<std::mutex> lock(g_settingsMutex);
-    g_settings = std::move(next);
+    {
+        std::lock_guard<std::mutex> lock(g_settingsMutex);
+        g_settings = std::move(next);
+    }
+    // A deliberate endpoint/model reload invalidates stale completions and
+    // permits an immediate attempt against the newly published settings.
+    std::lock_guard<std::mutex> lock(g_backoffMutex);
+    g_backoff.Reset();
 }

 OllamaEndpointSettings OllamaConfig_Snapshot()
@@ -250,6 +368,20 @@ OllamaApiResult QueryOllama(const std::string& prompt, OllamaRequestKind kind)
         return result;
     }

+    RealmBoxOllamaBackoff::Permit permit;
+    {
+        std::lock_guard<std::mutex> lock(g_backoffMutex);
+        permit = g_backoff.Acquire(BackoffNowMs());
+    }
+    if (!permit.allowed)
+    {
+        result.error = "local inference temporarily paused after repeated failures";
+        return result;
+    }
+    // The guard reports every exit, including exceptions. It does not hold a
+    // mutex during HTTP and adds no retry to upstream's bounded self-healing.
+    BackoffCompletion backoff{ permit };
+
     const OllamaEndpointSettings cfg = OllamaConfig_Snapshot();

     // One place decides what the "think" field should be: policy for this
@@ -344,6 +476,7 @@ OllamaApiResult QueryOllama(const std::string& prompt, OllamaRequestKind kind)
             LOG_INFO("module.ollamachat", "[Ollama Chat] Model reasoning: {}", result.thinking);
     }

+    backoff.successful = result.ok && !result.text.empty();
     return result;
 }

diff --git a/src/mod-ollama-chat_command.cpp b/src/mod-ollama-chat_command.cpp
index 22d03df..6bd9f0d 100644
--- a/src/mod-ollama-chat_command.cpp
+++ b/src/mod-ollama-chat_command.cpp
@@ -6,6 +6,7 @@
 #include "mod-ollama-chat_capability.h"
 #include "mod-ollama-chat_dispatch.h"
 #include "mod-ollama-chat_governor.h"
+#include "mod-ollama-chat_random.h"
 #include "mod-ollama-chat_response.h"
 #include "mod-ollama-chat_roleplay.h"
 #include "mod-ollama-chat-utilities.h"
@@ -64,6 +65,8 @@ bool OllamaChatConfigCommand::HandleOllamaReloadCommand(ChatHandler* handler)
     sConfigMgr->Reload();
     LoadOllamaChatConfig();
     Roleplay_Load();
+    OllamaDispatch_OnConfigReload();
+    OllamaRandomChatter_ResetSchedule();

     // Re-probe: the operator may have just pointed the module at a different
     // model, and think-mode support is per-model.
diff --git a/src/mod-ollama-chat_dispatch.cpp b/src/mod-ollama-chat_dispatch.cpp
index b58b76d..17d2c5f 100644
--- a/src/mod-ollama-chat_dispatch.cpp
+++ b/src/mod-ollama-chat_dispatch.cpp
@@ -24,6 +24,7 @@
 #include "PlayerbotAI.h"
 #include "PlayerbotMgr.h"

+#include <algorithm>
 #include <atomic>
 #include <chrono>
 #include <condition_variable>
@@ -43,6 +44,7 @@ namespace
     {
         TaskType          type = TaskType::ChatReply;
         OllamaChatRequest request;
+        uint64_t          ambientEpoch = 0;

         // Sentiment-only payload.
         uint64_t    sentimentBotGuid    = 0;
@@ -57,11 +59,25 @@ namespace
         std::string memoryPrompt;
     };

+    bool IsHumanReply(const Task& task)
+    {
+        // Human-originated chat replies are the only requests that record a
+        // player/bot turn. Bot chains, ambient lines, events and background
+        // maintenance never set this flag.
+        return task.type == TaskType::ChatReply && task.request.recordHistory;
+    }
+
+    bool IsAmbientChat(const Task& task)
+    {
+        return task.type == TaskType::ChatReply && !IsHumanReply(task);
+    }
+
     struct Completion
     {
         OllamaChatRequest request;
         std::string       text;
         uint32_t          emoteId = 0;
+        uint64_t          ambientEpoch = 0;
         Clock::time_point deliverAt;
     };

@@ -84,6 +100,7 @@ namespace
     std::atomic<uint64_t> g_droppedEmpty{ 0 };
     std::atomic<uint64_t> g_droppedGovernor{ 0 };
     std::atomic<uint64_t> g_totalFailed{ 0 };
+    std::atomic<uint64_t> g_ambientEpoch{ 1 };

     std::mutex  g_errorMutex;
     std::string g_lastError;
@@ -99,6 +116,10 @@ namespace

     void RunChatTask(const Task& task)
     {
+        if (IsAmbientChat(task) &&
+            task.ambientEpoch != g_ambientEpoch.load(std::memory_order_acquire))
+            return;
+
         OllamaApiResult api = QueryOllama(task.request.prompt, task.request.kind);

         if (!api.ok)
@@ -138,6 +159,7 @@ namespace
         completion.request = task.request;
         completion.text    = std::move(text);
         completion.emoteId = emoteId;
+        completion.ambientEpoch = task.ambientEpoch;

         uint32_t delayMs = 0;
         if (g_EnableTypingSimulation)
@@ -152,6 +174,9 @@ namespace
         completion.deliverAt = Clock::now() + std::chrono::milliseconds(delayMs);

         std::lock_guard<std::mutex> lock(g_doneMutex);
+        if (IsAmbientChat(task) &&
+            task.ambientEpoch != g_ambientEpoch.load(std::memory_order_acquire))
+            return;
         g_done.push_back(std::move(completion));
     }

@@ -326,6 +351,10 @@ namespace

     void Deliver(const Completion& c, const OllamaWorldSnapshot& world)
     {
+        if (!c.request.recordHistory &&
+            c.ambientEpoch != g_ambientEpoch.load(std::memory_order_acquire))
+            return;
+
         Player* bot = ObjectAccessor::FindConnectedPlayer(ObjectGuid(c.request.botGuid));
         if (!bot || !bot->IsInWorld())
             return;
@@ -338,7 +367,12 @@ namespace

         // Repetition is checked at delivery rather than at submission, because
         // we only know what the model actually said now.
-        if (Governor_IsRepetitive(botGuid, c.request.scopeKey, c.text))
+        // A real player's eligible message must not disappear because ambient
+        // chatter used the shared governor budget first. Human replies remain
+        // bounded by the dispatcher queue/worker pool, and may still fail if
+        // Ollama fails or the destination no longer exists.
+        const bool humanReply = c.request.recordHistory;
+        if (!humanReply && Governor_IsRepetitive(botGuid, c.request.scopeKey, c.text))
         {
             ++g_droppedGovernor;
             if (g_DebugEnabled)
@@ -348,7 +382,7 @@ namespace
             return;
         }

-        if (!Governor_TryConsumeSend(botGuid, c.request.scopeKey))
+        if (!humanReply && !Governor_TryConsumeSend(botGuid, c.request.scopeKey))
         {
             ++g_droppedGovernor;
             if (g_DebugEnabled)
@@ -468,6 +502,7 @@ bool OllamaDispatch_Submit(OllamaChatRequest request)
     Task task;
     task.type    = TaskType::ChatReply;
     task.request = std::move(request);
+    const bool humanReply = IsHumanReply(task);

     {
         std::lock_guard<std::mutex> lock(g_queueMutex);
@@ -475,17 +510,59 @@ bool OllamaDispatch_Submit(OllamaChatRequest request)
         if (!g_running)
             return false;

-        if (g_MaxQueueDepth > 0 && g_queue.size() >= g_MaxQueueDepth)
+        if (!humanReply)
+            task.ambientEpoch = g_ambientEpoch.load(std::memory_order_acquire);
+
+        if (g_MaxQueueDepth > 0)
         {
-            ++g_droppedQueueFull;
-            if (g_DebugEnabled)
-                LOG_INFO("module.ollamachat",
-                         "[Ollama Chat] Queue full ({}); dropping request for {}.",
-                         g_queue.size(), task.request.botName);
-            return false;
+            if (humanReply && g_queue.size() >= g_MaxQueueDepth)
+            {
+                // The last slot is reserved for a player reply. If older
+                // ambient work filled the queue before this policy was loaded,
+                // evict the newest non-human task rather than the player.
+                auto evict = g_queue.end();
+                for (auto it = g_queue.begin(); it != g_queue.end(); ++it)
+                    if (!IsHumanReply(*it))
+                        evict = it;
+
+                if (evict == g_queue.end())
+                {
+                    ++g_droppedQueueFull;
+                    return false; // bounded even under a flood of player chat
+                }
+
+                g_queue.erase(evict);
+                ++g_droppedQueueFull;
+            }
+            else if (!humanReply)
+            {
+                const size_t ambientLimit = g_MaxQueueDepth > 0
+                    ? static_cast<size_t>(g_MaxQueueDepth - 1)
+                    : 0;
+                if (g_queue.size() >= ambientLimit)
+                {
+                    ++g_droppedQueueFull;
+                    if (g_DebugEnabled)
+                        LOG_INFO("module.ollamachat",
+                                 "[Ollama Chat] Ambient queue limit ({}) reached; reserving one slot for player chat.",
+                                 ambientLimit);
+                    return false;
+                }
+            }
         }

-        g_queue.push_back(std::move(task));
+        if (humanReply)
+        {
+            // Stable priority: preserve FIFO among player replies, but put them
+            // ahead of every queued ambient/background task.
+            auto firstAmbient = std::find_if(g_queue.begin(), g_queue.end(),
+                [](const Task& queued) { return !IsHumanReply(queued); });
+            g_queue.insert(firstAmbient, std::move(task));
+        }
+        else
+        {
+            g_queue.push_back(std::move(task));
+        }
     }

     ++g_totalSubmitted;
@@ -493,6 +570,42 @@ bool OllamaDispatch_Submit(OllamaChatRequest request)
     return true;
 }

+void OllamaDispatch_OnConfigReload()
+{
+    uint64_t currentEpoch;
+    size_t queuedRemoved = 0;
+    size_t completedRemoved = 0;
+
+    {
+        std::lock_guard<std::mutex> lock(g_queueMutex);
+        currentEpoch = g_ambientEpoch.fetch_add(1, std::memory_order_acq_rel) + 1;
+
+        const size_t before = g_queue.size();
+        g_queue.erase(std::remove_if(g_queue.begin(), g_queue.end(), IsAmbientChat),
+                      g_queue.end());
+        queuedRemoved = before - g_queue.size();
+    }
+
+    {
+        std::lock_guard<std::mutex> lock(g_doneMutex);
+        const size_t before = g_done.size();
+        g_done.erase(
+            std::remove_if(g_done.begin(), g_done.end(),
+                [currentEpoch](const Completion& completion)
+                {
+                    return !completion.request.recordHistory &&
+                           completion.ambientEpoch != currentEpoch;
+                }),
+            g_done.end());
+        completedRemoved = before - g_done.size();
+    }
+
+    if (g_DebugEnabled && (queuedRemoved > 0 || completedRemoved > 0))
+        LOG_INFO("module.ollamachat",
+                 "[Ollama Chat] Config reload invalidated {} queued and {} completed ambient request(s).",
+                 queuedRemoved, completedRemoved);
+}
+
 void OllamaDispatch_SubmitSentiment(uint64_t botGuid, uint64_t playerGuid,
                                     const std::string& message)
 {
diff --git a/src/mod-ollama-chat_dispatch.h b/src/mod-ollama-chat_dispatch.h
index bbb43ba..5dc8ed4 100644
--- a/src/mod-ollama-chat_dispatch.h
+++ b/src/mod-ollama-chat_dispatch.h
@@ -81,6 +81,10 @@ void OllamaDispatch_Update(uint32_t diff);
 void OllamaDispatch_Start();
 void OllamaDispatch_Stop();

+// Preserve player replies across a config reload, but invalidate ambient chat
+// that was generated under the previous conversation mode.
+void OllamaDispatch_OnConfigReload();
+
 // Fire-and-forget sentiment analysis. Runs entirely on a worker: the sentiment
 // store is mutex-guarded in-memory state plus async DB writes, so it never
 // needs the world thread -- and it must never run on it, because the analysis
diff --git a/src/mod-ollama-chat_events.cpp b/src/mod-ollama-chat_events.cpp
index 867f116..aee8bae 100644
--- a/src/mod-ollama-chat_events.cpp
+++ b/src/mod-ollama-chat_events.cpp
@@ -15,6 +15,7 @@
 #include "AchievementMgr.h"
 #include "Containers.h"
 #include "GameObject.h"
+#include "Group.h"
 #include "Guild.h"
 #include "Item.h"
 #include "Log.h"
@@ -252,10 +253,14 @@ void OllamaBotEventChatter::DispatchGameEvent(Player* source, std::string type,
             isGuildEvent ? SRC_GUILD_LOCAL
                          : (partyAudience ? SRC_PARTY_LOCAL : SRC_SAY_LOCAL);

+        const uint32_t groupOrZoneId =
+            (source_ == SRC_PARTY_LOCAL || source_ == SRC_RAID_LOCAL) && bot->GetGroup()
+                ? bot->GetGroup()->GetGUID().GetCounter()
+                : bot->GetZoneId();
         const std::string scopeKey = Governor_MakeScopeKey(
             ChatChannelSourceLocalStr[source_], 0, "",
             source_ == SRC_GUILD_LOCAL ? bot->GetGuildId() : 0,
-            bot->GetZoneId());
+            groupOrZoneId);

         if (!Governor_CanSend(bot->GetGUID(), scopeKey))
             continue;
diff --git a/src/mod-ollama-chat_governor.cpp b/src/mod-ollama-chat_governor.cpp
index 9e0706a..57bd0a4 100644
--- a/src/mod-ollama-chat_governor.cpp
+++ b/src/mod-ollama-chat_governor.cpp
@@ -223,6 +223,11 @@ std::string Governor_MakeScopeKey(const char* sourceName, uint32_t channelId,
         key += "#g";
         key += std::to_string(guildId);
     }
+    else if (key == "Party" || key == "Raid")
+    {
+        key += "#p";
+        key += std::to_string(groupOrZoneId);
+    }
     else
     {
         key += "#z";
diff --git a/src/mod-ollama-chat_handler.cpp b/src/mod-ollama-chat_handler.cpp
index 81658e9..4e87519 100644
--- a/src/mod-ollama-chat_handler.cpp
+++ b/src/mod-ollama-chat_handler.cpp
@@ -1095,13 +1095,17 @@ void PlayerBotChatHandler::ProcessChat(Player* player, uint32_t /*type*/, uint32
     // --- conversation governor -------------------------------------------
     // One key per conversation space so cooldowns, rate limits and repetition
     // history are tracked per channel rather than globally.
+    const uint32_t groupOrZoneId =
+        (sourceLocal == SRC_PARTY_LOCAL || sourceLocal == SRC_RAID_LOCAL) && player->GetGroup()
+            ? player->GetGroup()->GetGUID().GetCounter()
+            : player->GetZoneId();
     const std::string scopeKey = Governor_MakeScopeKey(
         ChatChannelSourceLocalStr[sourceLocal],
         channel ? channel->GetChannelId() : 0,
         channel ? channel->GetName() : std::string(),
         (sourceLocal == SRC_GUILD_LOCAL || sourceLocal == SRC_OFFICER_LOCAL)
             ? player->GetGuildId() : 0,
-        player->GetZoneId());
+        groupOrZoneId);

     if (!senderIsBot)
     {
@@ -1634,7 +1638,7 @@ void PlayerBotChatHandler::ProcessChat(Player* player, uint32_t /*type*/, uint32
         // Everything below runs on the world thread: prompt building reads
         // live world state, and the governor decides before we spend an LLM
         // call rather than after.
-        if (!Governor_CanSend(bot->GetGUID(), scopeKey))
+        if (senderIsBot && !Governor_CanSend(bot->GetGUID(), scopeKey))
         {
             if (g_DebugEnabled)
                 LOG_INFO("module.ollamachat",
diff --git a/src/mod-ollama-chat_random.cpp b/src/mod-ollama-chat_random.cpp
index 710070b..8c489cb 100644
--- a/src/mod-ollama-chat_random.cpp
+++ b/src/mod-ollama-chat_random.cpp
@@ -14,6 +14,7 @@

 #include "Channel.h"
 #include "ChannelMgr.h"
+#include "Group.h"
 #include "Guild.h"
 #include "Log.h"
 #include "Map.h"
@@ -201,6 +202,12 @@ void OllamaRandomChatter_ForgetBot(ObjectGuid botGuid)
     g_nextRandomChatTime.erase(botGuid.GetRawValue());
 }

+void OllamaRandomChatter_ResetSchedule()
+{
+    std::lock_guard<std::mutex> lock(g_scheduleMutex);
+    g_nextRandomChatTime.clear();
+}
+
 OllamaBotRandomChatter::OllamaBotRandomChatter() : WorldScript("OllamaBotRandomChatter") { }

 void OllamaBotRandomChatter::OnUpdate(uint32 diff)
@@ -284,6 +291,12 @@ void OllamaBotRandomChatter::HandleRandomChatter()

     const time_t now = time(nullptr);

+    // This limit is deliberately local to one ambient-chatter pass and one
+    // destination scope. A busy party or city must not consume another
+    // audience's allowance, and a full dispatcher queue must not count as a
+    // submitted line.
+    std::unordered_map<std::string, uint32_t> successfulSubmissionsByScope;
+
     for (auto const& itr : allPlayers)
     {
         Player* bot = itr.second;
@@ -357,11 +370,25 @@ void OllamaBotRandomChatter::HandleRandomChatter()

         // Same inputs the reply path uses, so ambient lines and replies in the
         // same channel share one cooldown, rate limit and repetition history.
+        const uint32_t groupOrZoneId =
+            (source == SRC_PARTY_LOCAL || source == SRC_RAID_LOCAL) && bot->GetGroup()
+                ? bot->GetGroup()->GetGUID().GetCounter()
+                : bot->GetZoneId();
         const std::string scopeKey = Governor_MakeScopeKey(
             ChatChannelSourceLocalStr[source],
             channelId, channelName,
             source == SRC_GUILD_LOCAL ? bot->GetGuildId() : 0,
-            bot->GetZoneId());
+            groupOrZoneId);
+
+        const auto submittedIt = successfulSubmissionsByScope.find(scopeKey);
+        const uint32_t submittedForScope = submittedIt == successfulSubmissionsByScope.end()
+            ? 0
+            : submittedIt->second;
+        if (submittedForScope >= g_RandomChatterMaxBotsPerPlayer)
+        {
+            reschedule();
+            continue;
+        }

         if (!Governor_CanSend(bot->GetGUID(), scopeKey))
         {
@@ -390,7 +417,10 @@ void OllamaBotRandomChatter::HandleRandomChatter()
         request.triggerBotReplies = true;

         if (OllamaDispatch_Submit(std::move(request)))
+        {
             Topics_NoteUsed(bot->GetGUID(), topic.key);
+            ++successfulSubmissionsByScope[scopeKey];
+        }

         reschedule();
     }
diff --git a/src/mod-ollama-chat_random.h b/src/mod-ollama-chat_random.h
index c910af9..fa27e52 100644
--- a/src/mod-ollama-chat_random.h
+++ b/src/mod-ollama-chat_random.h
@@ -20,4 +20,8 @@ private:
 // bot GUID the server had ever seen and never released one.
 void OllamaRandomChatter_ForgetBot(ObjectGuid botGuid);

+// Clears every pending random-chatter deadline. Config hot reloads call this
+// after changing the min/max interval so bots do not keep stale deadlines.
+void OllamaRandomChatter_ResetSchedule();
+
 #endif // MOD_OLLAMA_CHAT_RANDOM_H
