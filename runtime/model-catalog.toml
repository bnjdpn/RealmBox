schema_version = 1
verified_at = "2026-09-02"

# Intentionally empty. A model becomes selectable only after its exact Ollama
# tag, downloadable size, licence, French quality, latency, memory footprint,
# and platform matrix have been measured. CanIRun integration is unresolved.

